{
  "name": "slack-giveaway",
  "scripts": {
    "build": "rm -rf build/* && tsc -p tsconfig.json",
    "start": "npm run build && node build/app.js",
    "test": "npm run build && node build/app.js --test"
  },
  "devDependencies": {
    "@types/body-parser": "^1.17.0",
    "@types/express": "^4.16.1",
    "@types/request-promise": "^4.1.42",
    "@types/yargs": "^12.0.10",
    "body-parser": "^1.18.3",
    "express": "^4.16.4",
    "fs": "0.0.1-security",
    "typescript": "^3.4.1"
  },
  "dependencies": {
    "@slack/interactive-messages": "^1.1.1",
    "@slack/web-api": "^5.0.0",
    "@types/signale": "^1.2.1",
    "ngrok": "^3.1.1",
    "signale": "^1.4.0",
    "yargs": "^13.2.2"
  }
}
